precision mediump float;

varying vec2 vUv;
varying float vIntensity;

uniform sampler2D u_Texture;
 
void main() {
	vec4 colorTxt = texture2D(u_Texture,vec2(vUv.x,1.0-vUv.y));
	
	vec3 ambient = 0.3 * vec3(colorTxt);
	
	gl_FragColor = vec4(ambient + vIntensity * vec3(colorTxt), 1.0);
}
